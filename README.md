# divi-semantic-fix
Adds `role="main"` to Divi's `&lt;div id="main-content">` element to improve accessibility.   This ensures the main content landmark is recognized by screen readers and accessibility tools.
